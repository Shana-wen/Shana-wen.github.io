sidebar:
    # Left sidebar configurations
    left:
        # Whether the sidebar sticks to the top when page scrolls
        sticky: false
    # Right sidebar configurations
    right:
        # Whether the sidebar sticks to the top when page scrolls
        sticky: true
# Sidebar widget configurations
# http://ppoffice.github.io/hexo-theme-icarus/categories/Widgets/
widgets:
    # # Profile widget configurations
    # -
    #     # Where should the widget be placed, left sidebar or right sidebar
    #     position: left
    #     type: profile
    #     # Author name
    #     author: Shana-wen
    #     # Author title
    #     author_title: 嵌入式软件工程师
    #     # Author's current location
    #     location: local
    #     # URL or path to the avatar image
    #     avatar: /img/Animedia_200712_sp1_OotsukaMai_Shana_vector_512.png
    #     # Whether show the rounded avatar image
    #     avatar_rounded: false
    #     # Email address for the Gravatar
    #     gravatar: 
    #     # URL or path for the follow button
    #     follow_link: https://github.com/Shana-wen
    #     # Links to be shown on the bottom of the profile widget
    #     social_links:
    #         Github:
    #             icon: fab fa-github
    #             url: https://github.com/Shana-wen
    #         # Facebook:
    #         #     icon: fab fa-facebook
    #         #     url: https://facebook.com
    #         # Twitter:
    #         #     icon: fab fa-twitter
    #         #     url: https://twitter.com
    #         # Dribbble:
    #         #     icon: fab fa-dribbble
    #         #     url: https://dribbble.com
    #         # RSS:
    #         #     icon: fas fa-rss
    #         #     url: /
    # # Tags widget configurations
    # -
    #     # Where should the widget be placed, left sidebar or right sidebar
    #     position: left
    #     type: tags
    #     # How to order tags. For example 'name' to order by name in ascending order, and '-length' to order by number of posts in each tags in descending order
    #     order_by: name
    #     # Amount of tags to show. Will show all if not set.
    #     amount: 
    #     # Whether to show tags count, i.e. number of posts in the tag.
    #     show_count: true
    # # Table of contents widget configurations
    -
        # Where should the widget be placed, left sidebar or right sidebar
        position: right
        type: toc
        # Whether to show the index of each heading
        index: true
        # Whether to collapse sub-headings when they are out-of-view
        collapsed: true
        # Maximum level of headings to show (1-6)
        depth: 3
    # Categories widget configurations
    -
        # Where should the widget be placed, left sidebar or right sidebar
        position: right
        type: categories
    # Recent posts widget configurations
    -
        # Where should the widget be placed, left sidebar or right sidebar
        position: right
        type: recent_posts
    # Archives widget configurations
    -
        # Where should the widget be placed, left sidebar or right sidebar
        position: right
        type: archives