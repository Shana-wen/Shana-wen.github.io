---
title: 打包程序和依赖库安装
date: 2026-04-27 09:43:40
tags: ['Linux', 'Shell']
categories: 嵌入式
toc: true
excerpt: Linux端打包程序和依赖库相关的笔记
---
# 打包程序

## copy.sh
```bash
#! /bin/bash


#执行脚本时在终端输入　./copy.sh ./test ./copy
#其中./test为ldd所要查看的可执行程序的路径，./copy为依赖文件最终拷贝到的文件夹路径

#以下三个参数为终端输入
# $0 为 ./copy.sh
# $1 为 ./test
# $2 为 ./copy

# awk 后　$0,$1,$2为终端日志的字符串，以空格为分隔符，只在awk本行起作用
#将字符串重定向到dependList
dependList=$( ldd $1 | awk '{if (match($3,"/")){ print $3}}' )

#输出　./test
#echo $1

#拷贝依赖
cp $dependList $2
#拷贝可执行程序
cp $1 $2
```

## 拷贝程序
```bash
sudo ./copy.sh name_proc ./dst_dir

cp config.json dst_dir/

scp -r dst_dir/ locate@192.168.6.62:
```

# 流转推
```bash
ffmpeg -re-i rtsp://src_url -c copy -f rtsp rtsp://dst_url
```

# 依赖库安装

## Boost
```bash
sudo apt-get update

# 依赖：
sudo apt install mpi-default-dev libicu-dev python-dev python3-dev libbz2-dev zlib1g-dev

cd boost_1_65_1
sudo ./bootstrap.sh
sudo ./b2 install
sudo ldconfig

cd ../
```

## Uhd
```C
sudo apt-get update

# 依赖：
sudo apt-get -y install git swig cmake doxygen build-essential libtool libusb-1.0-0 libusb-1.0-0-dev libudev-dev libncurses5-dev libfftw3-bin libfftw3-dev libfftw3-doc libcppunit-1.14-0 libcppunit-dev libcppunit-doc ncurses-bin cpufrequtils python-numpy python-numpy-doc python-numpy-dbg python-scipy python-docutils qt4-bin-dbg qt4-default qt4-doc libqt4-dev libqt4-dev-bin python-qt4 python-qt4-dbg python-qt4-dev python-qt4-doc python-qt4-doc libqwt6abi1 libfftw3-bin libfftw3-dev libfftw3-doc ncurses-bin libncurses5 libncurses5-dev libncurses5-dbg libfontconfig1-dev libxrender-dev libpulse-dev swig g++ automake autoconf libtool python-dev libfftw3-dev libcppunit-dev libusb-dev libusb-1.0-0-dev fort77 libsdl1.2-dev python-wxgtk3.0 git libqt4-dev python-numpy ccache python-opengl libgsl-dev python-cheetah python-mako python-lxml doxygen qt4-default qt4-dev-tools libusb-1.0-0-dev libqwtplot3d-qt5-dev pyqt4-dev-tools python-qwt5-qt4 cmake git wget libxi-dev gtk2-engines-pixbuf r-base-dev python-tk liborc-0.4-0 liborc-0.4-dev libasound2-dev python-gtk2 libzmq3-dev libzmq5 python-requests python-sphinx libcomedi-dev python-zmq libqwt-dev libqwt6abi1 python-six libgps-dev libgps23 gpsd gpsd-clients python-gps python-setuptools

sudo apt install gnuplot


git clone https://github.com/EttusResearch/uhd

cd uhd
git checkout v3.13.0.2 
cd host
mkdir build
cd build
cmake ../
sudo make
sudo make install
sudo ldconfig
sudo uhd_images_downloader
cd ../../
```

## zmq
```C
cd libzmq
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
sudo make
sudo make install
sudo ldconfig
cd ../../

cd cppzmq
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release -DCPPZMQ_BUILD_TESTS=OFF
sudo make
sudo make install
sudo ldconfig
cd ../../../
```

## Glog
```C
sudo apt install autoconf automake libtool

cd glog
mkdir build
cd build
cmake ../
sudo make
sudo make install
sudo ldconfig
cd ../../
```

## flatbuffers
```C
cd flatbuffers
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
sudo make
sudo make install
sudo ldconfig
cd ../../
```

## jsoncapp
```C
cd jsoncpp
mkdir build
cd build
cmake ../
sudo make
sudo make install
sudo ldconfig
cd ../../
```

## mosquitto
```C
# 依赖： 
sudo apt install -y libssl-dev 

cd mosquitto
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
sudo make
sudo make install
sudo ldconfig
cd ../../
```

## jwt-cpp
```C
cd jwt-cpp
mkdir build
cd build
cmake ../
sudo make
sudo make install
sudo ldconfig
cd ../../
```

## nlohmann_json
```C
cd nlohmann_json
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
sudo make
sudo make install
sudo ldconfig
cd ../../
```

## googletest
```C
cd googletest
mkdir build
cd build
cmake ../
sudo make
sudo make install
sudo ldconfig
cd ../../
```

## EasyConnect
```C
cd EasyConnect
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
sudo make
sudo make install
sudo ldconfig
cd ../../
```
